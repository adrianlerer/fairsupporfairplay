{
  "private": true,
  "name": "client",
  "description": "a completely private, decentralized and interactive AI companion",
  "version": "2.0.0",
  "author": "existence",
  "type": "module",
  "scripts": {
    "dev": "cd src/client && npm run dev",
    "build": "cd src/client && npm run build",
    "start": "cd src/client && npm run start",
    "postinstall": "cd src/client && npm install --legacy-peer-deps",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
